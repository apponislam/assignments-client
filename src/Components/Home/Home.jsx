// import { useContext } from "react";
// import { Context } from "../Provider/Provider";

const Home = () => {
    // const { name } = useContext(Context);
    // console.log(name);

    return (
        <div>
            <h1>This is home</h1>
        </div>
    );
};

export default Home;
